name: Deploy tag to environment

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Tag to deploy
        required: true
      environment:
        description: Environment where to deploy
        type: environment
        required: true
        default: dev

jobs:
  deploy:
    environment:
      name: ${{ github.event.inputs.environment }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ inputs.tag }}
      - name: Deploy ${{ github.event.inputs.tag }} to ${{ github.event.inputs.environment }}
        run: |
          echo Deploy ${{ github.event.inputs.tag }} to ${{ github.event.inputs.environment }}
          git log --pretty=oneline --decorate | head -n 1
