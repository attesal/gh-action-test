name: 'Release edited'

on:
  release:
    types: [edited]

jobs:
  onEdited:
    name: On edited
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Log out information about release
        run: |
          echo PreRelease ${{ github.event.release.prerelease }}
          git describe --tags
